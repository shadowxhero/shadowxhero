# 排除文件：以下内容只保留在本机，不进入版本库
#
# AGENTS.md 与 docs/superpowers 属于本机工作约定与设计文档，不应出现在远程仓库：
# 它们记录了本地 AI 协作流程与内部设计决策，对使用者没有价值，也不宜公开。

# 本机工作约定（AI 协作规则）
AGENTS.md

# 本机设计文档
docs/superpowers/

# 说明：这两个路径已从全部 Git 历史中移除。
# 若在其他机器克隆后需要它们，请从本地备份恢复，不要提交进仓库。
